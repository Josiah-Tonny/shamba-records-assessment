# Copy to .env locally (never commit real secrets).
DATABASE_URL=
JWT_SECRET=

# Vite: base URL for Vercel API functions (trailing slash optional).
# Local development: http://localhost:5173/api (when running: npm run dev)
# Production/Vercel: leave empty or use relative /api (automatic)
VITE_API_BASE_URL=
