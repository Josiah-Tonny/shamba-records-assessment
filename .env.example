# Copy to .env locally (never commit real secrets).
DATABASE_URL=
JWT_SECRET=

# Vite: base URL for Netlify Functions (trailing slash optional).
# Local with Netlify Dev: http://localhost:5174/.netlify/functions
# In production, leave empty to use relative Netlify function paths.
VITE_API_BASE_URL=
