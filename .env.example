# Copy to .env locally (never commit real secrets).
DATABASE_URL=
JWT_SECRET=

# Vite: base URL for Vercel API functions (trailing slash optional).
# Local with Vite dev server: http://localhost:5173/api
# In production, leave empty to use relative Vercel API paths.
VITE_API_BASE_URL=
